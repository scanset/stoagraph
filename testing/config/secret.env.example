# Copy to secret.env and set a real random value:  openssl rand -hex 32
# This is the HMAC secret the event-server signs events with and the ingress front door verifies.
# secret.env is gitignored (never commit it). The gate holds no model keys — only this shared secret.
SHARED_SECRET=replace-with-a-random-secret
