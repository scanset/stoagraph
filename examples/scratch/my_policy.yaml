recipe: my_policy
version: 1
rules:
  action.allowed:
    kind: set_membership
    set: ["safe_value"]
steps:
  - id: propose
    kind: propose
    out: v
  - id: apply
    kind: sink
    in: v
    field: mcp.tool.arg
    sensitivity: authoritative
    rule: action.allowed
    actor: "policy:example"
